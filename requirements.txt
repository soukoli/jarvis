# Jarvis — pinned, verified working set (macOS / Apple Silicon, Python 3.12)
# Regenerate after `pip install --upgrade ...` + smoke test.
#
# NOTE: torch/torchaudio must move together. torchaudio's latest release
# is 2.11.0 and silero-vad requires it — do not bump torch past that.

pynput==1.8.2
rumps==0.4.0
PyAudio==0.2.14
setproctitle==1.3.7
numpy==2.4.6
torch==2.11.0
torchaudio==2.11.0
mlx==0.32.2
mlx-whisper==0.4.3
faster-whisper==1.2.1
silero-vad==6.2.3
huggingface_hub==1.32.0
pyobjc-framework-Cocoa==12.2.2
pyobjc-framework-AVFoundation==12.2.2
pyobjc-framework-ApplicationServices==12.2.2
